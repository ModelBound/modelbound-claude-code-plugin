---
description: Pull team skills and rules from ModelBound into ./.claude/
---

Pulls the active team's skills, rules, and system prompts from ModelBound.co into the current project's `.claude/` directory.

!`node ${CLAUDE_PLUGIN_ROOT}/dist/sync-rules.js`
