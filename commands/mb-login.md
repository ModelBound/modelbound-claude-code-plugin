---
description: Authenticate the ModelBound CLI via device-code flow.
allowed-tools: Bash(npx:*)
---

Run: `npx -y @modelbound/cli login`

Show the verification URL and code prominently so the user can paste it.
